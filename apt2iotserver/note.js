/* 
HTTPS: configured cloudfront on aws with load balancer
or,to create a new self signed ssl:
openssl req -x509 -newkey rsa:4096 -nodes -keyout key.pem -out cert.pem -days 365
chmod 400 server.key.pem


Next, redesign bottom bar, and implement qr code functionality


*/
